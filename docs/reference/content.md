# `@emi/content`

The exported surface of `packages/content`, generated by `npm run generate:reference`.
Nobody writes this page by hand. The prose under each symbol is the documentation comment that
sits on it in the source, so this page cannot say one thing while the code says another.

21 exported symbols, across 5 files.

## `packages/content/src/index.ts`

### `packageName`

```
const packageName = '@emi/content'
```

The application imports this to prove the workspace resolves at run time. A type import cannot
do it, because the types come off before the bundle runs.

## `packages/content/src/article.ts`

### `Article`

```
interface Article {
  /** Stable across a rewording, so a reader who dismissed this one is not shown it again. */
  readonly id: string;
  /** The phase it was asked for. An article answering about another phase is refused. */
  readonly phase: PhaseName;
  readonly title: string;
  readonly body: string;
  /** Who wrote it and who, if anybody, read it after them. Drawn under the body. */
  readonly attribution: string;
  /**
   * Where the whole piece is, or nothing where there is no page to send her to. A piece with
   * nothing here is drawn without the control that opens one.
   */
  readonly link: string | null;
}
```

One article. Every field is drawn, so an absent field is an article a screen cannot draw and is
refused before it reaches one.

### `ArticleSource`

```
type ArticleSource = (phase: PhaseName) => Promise<Article | null>;
```

Where an article comes from. A screen calls this and never knows what answered it.

### `articleFrom`

```
function articleFrom(given: unknown, asked: PhaseName): Article | null
```

The shape check every answer passes, whatever answered. It returns nothing rather than throwing.
A server can change a field at any time. That is never a reason for a screen to fail.

The phase is checked against the phase that was asked for. A source that answers about the wrong
phase is worse than a source that answers nothing, because the words would be drawn under the
wrong heading and read as true.

## `packages/content/src/cache.ts`

### `CACHE_LIFETIME_MILLISECONDS`

```
const CACHE_LIFETIME_MILLISECONDS = 12 * 60 * 60 * 1000
```

Milliseconds. Twelve hours, so a phase that turns overnight is read again in the morning.

### `CachedAnswer`

```
interface CachedAnswer {
  readonly phase: PhaseName;
  /** Null where the source answered nothing, which is remembered rather than retried at once. */
  readonly article: Article | null;
  /** When it was read, as an instant in the format the rest of Emi writes instants in. */
  readonly readAt: string;
}
```

One answer as it is held between draws.

### `ArticleCache`

```
interface ArticleCache {
  read(): Promise<CachedAnswer | null>;
  write(answer: CachedAnswer): Promise<void>;
}
```

Where an answer is held on the device. The application supplies one built on its own storage.

### `isFresh`

```
function isFresh(answer: CachedAnswer, phase: PhaseName, now: Date): boolean
```

Whether a held answer can stand in for a call: it is about the phase being asked about, it was
read no longer ago than the lifetime, and it was not read after now. A clock that has moved
backwards makes the last of those true, and the answer is dropped rather than trusted.

### `writtenAnswer`

```
function writtenAnswer(answer: CachedAnswer): string
```

An answer written for storage, which is one line of json.

### `readAnswer`

```
function readAnswer(written: string): CachedAnswer | null
```

An answer read back from storage, or nothing. Storage is read as untrusted: a line that is not
json, or that holds something other than the shape written above, answers nothing rather than
throwing on the draw that reads it.

### `memoryCache`

```
function memoryCache(held: CachedAnswer | null = null): ArticleCache
```

A cache held in this process, which is what a test drives and what a screen starts with.

## `packages/content/src/client.ts`

### `ARTICLE_TIMEOUT_MILLISECONDS`

```
const ARTICLE_TIMEOUT_MILLISECONDS = 4000
```

Milliseconds. An article nobody has answered by now is an article a screen is better off without,
because the rest of the screen is her own data and is already drawn.

### `articlePath`

```
function articlePath(phase: PhaseName): string
```

The path a phase is read from. The phase is the whole of the request.

### `ArticleRequest`

```
interface ArticleRequest {
  readonly url: string;
  readonly method: string;
}
```

A request as this module makes one, which is a method and an address.

### `ArticleAnswer`

```
interface ArticleAnswer {
  readonly status: number;
  readonly body: unknown;
}
```

What came back: the status, and the body already read as json.

### `SendArticleRequest`

```
type SendArticleRequest = (request: ArticleRequest) => Promise<ArticleAnswer>;
```

All this module is given of the network, so a test drives the real client and no socket.

### `fetchSender`

```
const fetchSender: SendArticleRequest
```

The sender the application runs on, which is the platform's own.

### `withoutATrailingSlash`

```
function withoutATrailingSlash(address: string): string
```

An address with no trailing slash, so joining a path to it cannot produce two.

### `articlesAt`

```
function articlesAt(address: string | null, send: SendArticleRequest, waitMilliseconds: number = ARTICLE_TIMEOUT_MILLISECONDS): ArticleSource
```

The source that reads from the endpoint at that address.

An address of nothing is a build that has no endpoint to read, so the source answers nothing and
the sender is never called. Every other way this can go wrong ends in the same answer: a refusal,
a body that is not an article, an article about another phase, a call that threw, and a call
still running when the wait is over.

## `packages/content/src/reader.ts`

### `ArticleReaderParts`

```
interface ArticleReaderParts {
  /** Where an article is read from, which is the endpoint the build is pointed at. */
  readonly source: ArticleSource;
  readonly cache?: ArticleCache;
  readonly now?: () => Date;
}
```

What a reader is built from. Only the source has to be given.

### `articleReader`

```
function articleReader(parts: ArticleReaderParts): ArticleSource
```

A reader over those parts.

A held answer is used as it is, including a held nothing, so an endpoint that is down is asked
once and not once a draw.

Every part is called through `quietly`, so a storage that will not open and a source somebody
wrote badly both end where a refused call ends, which is no article at all.
