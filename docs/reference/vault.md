# `@emi/vault`

The exported surface of `services/vault`, generated by `npm run generate:reference`.
Nobody writes this page by hand. The prose under each symbol is the documentation comment that
sits on it in the source, so this page cannot say one thing while the code says another.

60 exported symbols, across 10 files.

## `services/vault/src/index.ts`

### `packageName`

```
const packageName = '@emi/vault'
```

The vault service. It holds ciphertext and it holds no key, so everything here is about who is
asking rather than about what she wrote.

## `services/vault/src/api.ts`

### `HttpRequestEvent`

```
interface HttpRequestEvent {
  readonly rawPath: string;
  readonly rawQueryString?: string;
  readonly headers: Readonly<Record<string, string | undefined>>;
  readonly requestContext: {
    readonly http: { readonly method: string };
    /** What the authorizer handed on. A route with no authorizer leaves it absent. */
    readonly authorizer?: { readonly lambda?: Readonly<Record<string, string>> };
  };
  readonly body?: string;
  readonly isBase64Encoded?: boolean;
}
```

What reaches the vault function: payload format 2.0, with the body.

### `AuthorizerEvent`

```
type AuthorizerEvent = Omit<HttpRequestEvent, 'body' | 'isBase64Encoded'>;
```

What reaches the authorizer. It is the request without its body, which is why the digest of the
body travels in a header: the authorizer could not compute one if it wanted to.

### `HttpResponse`

```
interface HttpResponse {
  readonly statusCode: number;
  readonly headers: Readonly<Record<string, string>>;
  readonly body: string;
}
```

What the vault function answers with.

### `AuthorizerResult`

```
interface AuthorizerResult {
  readonly isAuthorized: boolean;
  readonly context?: Readonly<Record<string, string>>;
}
```

What the authorizer answers with. The api is configured for simple responses, so this is a yes or
a no and nothing else, and the account travels to the function in the context.

### `refused`

```
const refused: AuthorizerResult
```

The refusal an authorizer gives. It carries no reason, so no caller learns why it was refused.

### `signedPathOf`

```
function signedPathOf(event: AuthorizerEvent | HttpRequestEvent): string
```

What was signed. The query string is part of it, so a caller cannot move a cursor or a page size
under a signature that was made for a different one.

### `methodOf`

```
function methodOf(event: AuthorizerEvent | HttpRequestEvent): string
```

The method, uppercase, as the signing string writes it.

### `bodyTextOf`

```
function bodyTextOf(event: HttpRequestEvent): string
```

The body as text. The api gateway sends base 64 when it decides the body is binary, so a reader
that ignored the flag would hash the wrong bytes for some requests and not for others.

### `answer`

```
function answer(statusCode: number, body: unknown): HttpResponse
```

An answer with a json body.

### `refusal`

```
function refusal(statusCode: number, message: string): HttpResponse
```

A refusal, as a status and a message. The message is the whole of what a caller is told: no field
name, no account, no hint about which of the checks it was that said no.

## `services/vault/src/auth/authorizer.ts`

### `accountIdContextKey`

```
const accountIdContextKey = 'accountId'
```

What an allowed request hands on to the function: the account, and nothing else about her.

### `authorizeRequest`

```
function authorizeRequest(event: AuthorizerEvent, store: AccountStore, now: Date): Promise<AuthorizerResult>
```

The order of the checks is the contract. The signature is verified before the replay store is
written, so a caller who cannot sign can never fill the store. An unknown account is verified
against a decoy key instead of being answered early, so the work, and therefore the time, is the
same as a bad signature on an account that does exist.

### `authorizer`

```
function authorizer(store: AccountStore, clock: () => Date = () => new Date()): (event: AuthorizerEvent) => Promise<AuthorizerResult>
```

The function the api calls, with its storage bound to it.

### `authorizedAccountIn`

```
function authorizedAccountIn(event: HttpRequestEvent): string | null
```

Who the authorizer said this is, read by the function behind it. A request that reaches a
handler without passing the authorizer has no account here, and the handler refuses rather than
trusting the header, so a route wired without its authorizer serves nobody.

## `services/vault/src/auth/signedBody.ts`

### `signedBodyRefusal`

```
function signedBodyRefusal(event: HttpRequestEvent): HttpResponse | null
```

The half of contract AUTH-1 the authorizer cannot do. A request authorizer receives no body, so
the digest of the body is signed and sent in a header, and the function that does receive the
body compares the two. Without this, a caller could keep a signature and change what it carries.

## `services/vault/src/handlers/pullRecords.ts`

### `pageBytes`

```
const pageBytes = 1024 * 1024
```

Bytes. A page stops before it costs more than this and answers with a cursor, because an answer
that truncated in silence would lose the records past the cut and nobody would know.

### `cursorParameter`

```
const cursorParameter = 'cursor'
```

The name of the one parameter a pull takes.

### `cursorFor`

```
function cursorFor(accountId: string, instant: string): string
```

The instant a cursor stands for, carried with the account it was made for. The account is in
there so a cursor kept from another account is refused rather than quietly read as an instant:
the query is bound to the caller either way, so this catches the restore that went wrong and
never an attack.

### `ReadCursor`

```
interface ReadCursor {
  readonly accountId: string;
  readonly instant: string;
}
```

What a cursor says once it is read back: whose it is, and how far it reached.

### `cursorParts`

```
function cursorParts(cursor: string): ReadCursor | null
```

The two halves of a cursor, or nothing at all when the text is not one.

### `pullRecordsFor`

```
function pullRecordsFor(event: HttpRequestEvent, store: RecordStore): Promise<HttpResponse>
```

She asks for everything written since the cursor she holds. A page carries the records and the
cursor to ask with next, and `moreToCome` says whether asking again now is worth a request.

### `pullRecords`

```
function pullRecords(store: RecordStore): (event: HttpRequestEvent) => Promise<HttpResponse>
```

The route the api calls, with its storage bound to it.

## `services/vault/src/handlers/putRecord.ts`

### `maximumEnvelopeBytes`

```
const maximumEnvelopeBytes = 65536
```

Bytes. An envelope larger than this is refused, which is contract WIRE-2.

### `maximumRecordBodyBytes`

```
const maximumRecordBodyBytes = Math.ceil(maximumEnvelopeBytes / 3) * 4 + 1024
```

Bytes. The body carries the envelope as base 64 and a little json around it, so this is the
envelope limit with room for the encoding, and it is read before the body is parsed.

### `recordIdPattern`

```
const recordIdPattern = /^[0-9a-f]{8}-[0-9a-f]{4}-7[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/
```

The identifier is the one part of a record that travels in the clear, so its shape is held to a
universally unique identifier of version 7 and nothing else. A free text identifier would be a
place to write a date, and a date is the thing the server may never learn.

### `recordPathPrefix`

```
const recordPathPrefix = '/v1/records/'
```

The path a record is written to, ending in the identifier this handler reads back out.

### `recordIdIn`

```
function recordIdIn(event: HttpRequestEvent): string | null
```

The identifier in the path, or nothing at all when the path does not carry a usable one.

### `putRecordFor`

```
function putRecordFor(event: HttpRequestEvent, store: RecordStore, now: Date): Promise<HttpResponse>
```

She writes a day. The revision she sends has to be higher than the one already there, and when
it is not the write is refused with the revision that is: two phones then converge on the higher
one rather than on whichever arrived last.

### `putRecord`

```
function putRecord(store: RecordStore, clock: () => Date = () => new Date()): (event: HttpRequestEvent) => Promise<HttpResponse>
```

The route the api calls, with its storage bound to it.

## `services/vault/src/handlers/register.ts`

### `maximumBodyBytes`

```
const maximumBodyBytes = 4096
```

Bytes. A body larger than this is refused before it is read, which is contract WIRE-1.

### `registerAccount`

```
function registerAccount(event: HttpRequestEvent, store: AccountStore, now: Date): Promise<HttpResponse>
```

She sends a public key, signed by the key she sent, with her wrapped vault key and her salt.
The handler derives the account identifier from that key, so she does not choose it and two
women cannot land on one.

### `register`

```
function register(store: AccountStore, clock: () => Date = () => new Date()): (event: HttpRequestEvent) => Promise<HttpResponse>
```

The route the api calls, with its storage bound to it.

## `services/vault/src/store/accounts.ts`

### `StoredAccount`

```
interface StoredAccount {
  readonly accountId: string;
  readonly publicKey: Uint8Array;
  /**
   * Her vault key, sealed under the key her 26 written characters derive. The service holds these
   * bytes and no way at all to open them, which is the same sentence as the one about her days.
   */
  readonly wrappedVaultKey: Uint8Array;
  /** The sixteen bytes her code was derived with. Useless on its own, and useless with these. */
  readonly recoverySalt: Uint8Array;
  readonly createdAt: string;
  readonly recordCount: number;
}
```

The account item of section 6.4 of the design, as far as this step builds it.

### `CreateOutcome`

```
type CreateOutcome = 'created' | 'already-registered';
```

What a create answers. A conditional write refuses a second account under one identifier, so the
caller reads an outcome rather than catching the store's own error.

### `RememberOutcome`

```
type RememberOutcome = 'remembered' | 'seen-before';
```

What a remember answers. A signature that was already written back is a replayed request.

### `AccountStore`

```
interface AccountStore {
  /** The account, or nothing at all when no account holds that identifier. */
  readAccount(accountId: string): Promise<StoredAccount | undefined>;
  /** Writes the account only when the identifier is free, and says which of the two happened. */
  createAccount(account: StoredAccount): Promise<CreateOutcome>;
  /**
   * Writes the signature down and says whether it was already there. The instant window is what
   * bounds how long a signature has to be remembered for, so `expiresAt` is a cleanup and never the
   * defence: a store that forgot early would still refuse the request, because the window refuses
   * it first.
   */
  rememberSignature(
    accountId: string,
    signature: string,
    expiresAt: number,
  ): Promise<RememberOutcome>;
}
```

The three things this step needs from storage. Every one of them is a single item operation, so
nothing here needs a scan and nothing here reads another account.

## `services/vault/src/store/dynamo.ts`

### `AttributeValue`

```
type AttributeValue =
  | { readonly S: string }
  | { readonly N: string }
  | { readonly B: Uint8Array }
  | { readonly BOOL: boolean };
```

The four value shapes this table uses. A record is bytes, a number and two strings.

### `Item`

```
type Item = Readonly<Record<string, AttributeValue>>;
```

An item as the table holds it: attribute names against attribute values.

### `ExpressionParts`

```
interface ExpressionParts {
  readonly ExpressionAttributeNames?: Readonly<Record<string, string>>;
  readonly ExpressionAttributeValues?: Readonly<Record<string, AttributeValue>>;
}
```

Names and values an expression refers to, as DynamoDB takes them.

### `PutItemInput`

```
interface PutItemInput extends ExpressionParts {
  readonly TableName: string;
  readonly Item: Item;
  readonly ConditionExpression?: string;
}
```

A write of one whole item, refused when the condition does not hold.

### `GetItemInput`

```
interface GetItemInput {
  readonly TableName: string;
  readonly Key: Item;
  readonly ConsistentRead?: boolean;
}
```

A read of one item by its whole key.

### `UpdateItemInput`

```
interface UpdateItemInput extends ExpressionParts {
  readonly TableName: string;
  readonly Key: Item;
  readonly UpdateExpression: string;
  readonly ConditionExpression?: string;
}
```

A change to part of one item, refused when the condition does not hold.

### `QueryInput`

```
interface QueryInput extends ExpressionParts {
  readonly TableName: string;
  readonly IndexName?: string;
  readonly KeyConditionExpression: string;
  readonly ExclusiveStartKey?: Item;
}
```

A read along a key, inside one partition.

### `QueryOutput`

```
interface QueryOutput {
  readonly Items: readonly Item[];
  readonly LastEvaluatedKey?: Item;
}
```

What a query answers: the items it read, and where to start again when it stopped early.

### `DynamoDbTable`

```
interface DynamoDbTable {
  putItem(input: PutItemInput): Promise<void>;
  getItem(input: GetItemInput): Promise<Item | undefined>;
  updateItem(input: UpdateItemInput): Promise<void>;
  query(input: QueryInput): Promise<QueryOutput>;
}
```

The four operations the vault uses. Every one of them is the shape DynamoDB takes, so the client
that binds this to the real table adds no behaviour of its own and can hide no difference.

### `conditionalCheckFailure`

```
const conditionalCheckFailure = 'ConditionalCheckFailedException'
```

What DynamoDB calls the error it raises when a condition does not hold.

### `isConditionalCheckFailure`

```
function isConditionalCheckFailure(thrown: unknown): boolean
```

True when a write was refused by its condition, which is an answer here rather than a fault.

### `partitionFor`

```
function partitionFor(accountId: string): string
```

The partition one account's items sit in, and nothing of hers sits anywhere else.

### `accountSortKey`

```
const accountSortKey = 'META'
```

The sort key of the account item. One account holds exactly one of these.

### `recordSortKeyFor`

```
function recordSortKeyFor(recordId: string): string
```

The sort key of a record item, which carries the identifier the phone chose.

### `signatureSortKeyFor`

```
function signatureSortKeyFor(signature: string): string
```

The sort key a signature is remembered under, so a replay meets an item that already exists.

### `byUpdatedIndex`

```
const byUpdatedIndex = 'byUpdated'
```

The index a pull reads: one account's records, in the order they were written.

### `dynamoStore`

```
function dynamoStore(table: DynamoDbTable, tableName: string): AccountStore & RecordStore
```

The storage the handlers are given, bound to one table. Nothing here reads an account other than
the one it was asked for, and nothing here holds a key: the payload goes in and comes out as the
bytes the phone sealed.

## `services/vault/src/store/records.ts`

### `StoredRecord`

```
interface StoredRecord {
  readonly recordId: string;
  readonly revision: number;
  readonly payload: Uint8Array;
  readonly updatedAt: string;
}
```

A record item of section 6.4 of the design, as the service holds it.

### `WriteOutcome`

```
type WriteOutcome =
  | { readonly outcome: 'written'; readonly revision: number; readonly created: boolean }
  | { readonly outcome: 'revision-is-not-higher'; readonly revision: number };
```

What a write answers. Both shapes carry a revision: the one now written, or the higher one that
is already there, so the phone learns what it is behind rather than only that it was refused.

### `RecordPage`

```
interface RecordPage {
  readonly records: readonly StoredRecord[];
  /** The write instant the page reached, or nothing at all when the page read no record. */
  readonly reached: string | null;
  readonly moreToCome: boolean;
}
```

One answer to a pull: the records read, how far the read reached, and whether more waits.

### `recordOverheadBytes`

```
const recordOverheadBytes = 160
```

Bytes a record costs a page on top of its ciphertext: the identifier, the revision, the instant
and the punctuation around them, as the answer writes them. It is an allowance rather than a
measurement, and it is generous, because the page limit exists to keep an answer under a size
the caller can hold.

### `pageCostOf`

```
function pageCostOf(record: StoredRecord): number
```

What one record costs a page. The ciphertext travels as base 64, which is four characters for
every three bytes, so the cost is counted in the answer's bytes and not in the table's.

### `RecordStore`

```
interface RecordStore {
  /** Writes the record when its revision is higher than the stored one, and says which happened. */
  writeRecord(accountId: string, record: StoredRecord): Promise<WriteOutcome>;
  /**
   * Reads the records written after an instant, in write order, stopping before the page costs
   * more than the budget. Records sharing one instant are never split across two pages, because a
   * cursor is an instant and a split group would lose whatever fell on the far side of it.
   */
  readRecordsAfter(
    accountId: string,
    after: string | null,
    atMostBytes: number,
  ): Promise<RecordPage>;
}
```

The two things a record needs from storage. Neither one reads another account, and neither one
scans: a write is a single item and a pull is a query inside one partition.
