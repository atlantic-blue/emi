# `@emi/crypto`

The exported surface of `packages/crypto`, generated by `npm run generate:reference`.
Nobody writes this page by hand. The prose under each symbol is the documentation comment that
sits on it in the source, so this page cannot say one thing while the code says another.

83 exported symbols, across 7 files.

## `packages/crypto/src/index.ts`

### `packageName`

```
const packageName = '@emi/crypto'
```

The application imports this to prove the workspace resolves at run time. A type import cannot
do it, because the types come off before the bundle runs.

## `packages/crypto/src/base64.ts`

### `Base64Refusal`

```
type Base64Refusal = 'text-is-not-base64';
```

The one way base 64 refuses, as a value, so a caller matches on the reason and not on words.

### `Base64Error`

```
class Base64Error extends Error {
  readonly refusal: Base64Refusal;
  constructor(message: string);
}
```

Carries the refusal beside the message, the way the envelope and the signature do.

### `base64Of`

```
function base64Of(bytes: Uint8Array): string
```

Bytes to text, padded with `=` so the length always reaches a multiple of four.

### `bytesFromBase64`

```
function bytesFromBase64(text: string): Uint8Array
```

Text to bytes. Anything outside the alphabet is refused rather than skipped, because a public key
that quietly loses a character is a public key that verifies nothing.

## `packages/crypto/src/canonical.ts`

### `JsonValue`

```
type JsonValue =
  | string
  | number
  | boolean
  | null
  | readonly JsonValue[]
  | { readonly [key: string]: JsonValue | undefined };
```

What may go inside an envelope. A date, a map or a class instance is not here, because it would
be written as an empty object and a reader would never know that anything was lost.

### `CanonicalRefusal`

```
type CanonicalRefusal = 'value-is-not-json' | 'number-is-not-finite';
```

The two ways this refuses, as values, so a caller matches on the reason rather than on the words
of a message.

### `CanonicalError`

```
class CanonicalError extends Error {
  readonly refusal: CanonicalRefusal;
  constructor(refusal: CanonicalRefusal, message: string);
}
```

Carries the path to the value that stopped it, such as `the record.symptoms[1]`, because one bad
field in a day is hard to find from a message alone.

### `canonicalJson`

```
function canonicalJson(value: JsonValue): string
```

Keys sorted and no whitespace, so one record gives one text on every phone and in every version.
That is what lets a vector checked into this repository still mean something next year.

### `canonicalBytes`

```
function canonicalBytes(value: JsonValue): Uint8Array
```

What the cipher seals. The text is turned into bytes the one way `TextEncoder` turns it.

### `fromCanonicalBytes`

```
function fromCanonicalBytes(bytes: Uint8Array): unknown
```

Reads bytes back as a value, and says `unknown` on purpose: bytes that came out of an envelope
are not a record until something has read them against one.

## `packages/crypto/src/envelope.ts`

### `envelopeVersion`

```
const envelopeVersion = 0x01
```

The bytes of section 7.2 of the design, in order: one byte of version, then a 24 byte nonce,
then the ciphertext and its 16 byte authentication tag. One implementation, used by the phone
and read by the service, so a change to the format can only happen in one place.

### `keyLength`

```
const keyLength = 32
```

Bytes. The cipher takes one key length and refuses every other.

### `nonceLength`

```
const nonceLength = 24
```

Bytes. A nonce this long can be drawn at random for every write without ever repeating, which is
what lets one key seal every day she logs.

### `tagLength`

```
const tagLength = 16
```

Bytes, at the end of the sealed part. One changed bit anywhere before it fails the tag, so a
record that somebody edited does not open at all.

### `headerLength`

```
const headerLength = 1 + nonceLength
```

The version byte and the nonce, before the ciphertext starts.

### `EnvelopeRefusal`

```
type EnvelopeRefusal =
  | 'key-is-the-wrong-length'
  | 'random-source-is-missing'
  | 'random-source-is-the-wrong-length'
  | 'nonce-is-all-zero'
  | 'envelope-is-truncated'
  | 'version-is-not-known'
  | 'envelope-is-not-authentic';
```

Every way a seal or an open refuses, as a value, so a caller matches on the reason rather than
on the words of a message.

### `EnvelopeError`

```
class EnvelopeError extends Error {
  readonly refusal: EnvelopeRefusal;
  constructor(refusal: EnvelopeRefusal, message: string);
}
```

Carries the refusal beside the message, so a truncated envelope and a changed one can be told
apart by the code that catches them.

### `RandomSource`

```
type RandomSource = (byteCount: number) => Uint8Array;
```

Where the nonce comes from. The phone passes the platform's generator, a vector passes bytes.

### `EnvelopeParts`

```
interface EnvelopeParts {
  readonly version: number;
  readonly nonce: Uint8Array;
  /** The ciphertext and the tag together, which is what the cipher reads. */
  readonly sealed: Uint8Array;
}
```

What the service is allowed to see: the version and the nonce, and the sealed bytes that it
holds no key to.

### `SealOptions`

```
interface SealOptions {
  /** Where the nonce comes from. Left out, it is the platform's own generator. */
  readonly random?: RandomSource;
  /** The symptom catalogue the slugs are read against. Left out, it is the one that ships. */
  readonly catalogue?: readonly Symptom[];
}
```

The two seams a test reaches for: where the nonce comes from, and which catalogue is read.

### `sealRecord`

```
function sealRecord(record: DayRecord, key: Uint8Array, options: SealOptions = {}): Uint8Array
```

The only way to write an envelope, so nothing can be sealed that has not passed the ranges of a
day first.

### `openRecord`

```
function openRecord(envelope: Uint8Array, key: Uint8Array): DayRecord
```

Reads a day back out. The ranges are not checked again here: a day she wrote years ago is hers
to read, whatever the catalogue holds now.

### `sealVaultKey`

```
function sealVaultKey(vaultKey: Uint8Array, under: Uint8Array, random: RandomSource = systemRandom): Uint8Array
```

The one other thing Emi seals: a vault key, under the key her recovery code derives. Thirty two
bytes exactly, so this cannot become a second way to write a day that skipped the ranges of
section 6.2. Those two are the whole list of what an envelope may carry.

### `openBytes`

```
function openBytes(envelope: Uint8Array, key: Uint8Array): Uint8Array
```

Refuses the same way whether the bytes were changed or the key is wrong, because telling the two
apart would say which of them somebody got right.

### `readEnvelope`

```
function readEnvelope(envelope: Uint8Array): EnvelopeParts
```

What the service is allowed to do with an envelope. It reads the shape and never the content,
because it holds no key and this repository is the proof of that.

### `systemRandom`

```
function systemRandom(byteCount: number): Uint8Array
```

The platform's own generator, and what a seal draws from when nothing else is handed in. A
runtime that has none refuses here rather than falling back to something weaker.

## `packages/crypto/src/record.ts`

### `DayRecord`

```
interface DayRecord {
  readonly day: string;
  readonly flow?: Flow;
  readonly bleedingIsUnexpected?: boolean;
  readonly symptoms?: readonly string[];
  readonly moods?: readonly string[];
  readonly energy?: number;
  readonly temperatureCelsius?: number;
  readonly weightKilograms?: number;
  readonly note?: string;
  readonly recordedAt: string;
}
```

The plaintext of a day, from section 6.2 of the design. Every range here is checked before a
record is sealed, because a number that is out of range is a number she typed by accident and
the envelope is the last place it can be caught while it is still readable.

### `flowValues`

```
const flowValues = ['none', 'spotting', 'light', 'medium', 'heavy'] as const satisfies readonly Flow[]
```

Every value of `Flow`, as data, because a type cannot be read at run time. The test holds the
assertion that this list covers the type, so a sixth value cannot arrive without landing here.

### `recordKeys`

```
const recordKeys: readonly string[]
```

Every key a record may carry. A key outside this list is refused rather than sealed.

### `lowestEnergy`

```
const lowestEnergy = 1
```

Five steps begin here. A scale she can answer in one tap beats one she has to think about.

### `highestEnergy`

```
const highestEnergy = 5
```

And they end here. A whole number only, because half a step is a precision she does not have.

### `longestNote`

```
const longestNote = 2000
```

Characters, counted in code points rather than in bytes, so an accented letter costs her one and
not two.

### `RecordError`

```
class RecordError extends Error {
  readonly problems: readonly string[];
  constructor(problems: readonly string[]);
}
```

Holds every problem the day carries, not the first one, so the message and the list are one
failure read two ways.

### `recordProblems`

```
function recordProblems(record: DayRecord, catalogue?: readonly Symptom[]): readonly string[]
```

Every offence, not the first one, so a screen can show her everything that is wrong in one pass.
The catalogue is a parameter because a test reads a record against the catalogue of a later year.

### `checkedRecord`

```
function checkedRecord(record: DayRecord, catalogue?: readonly Symptom[]): DayRecord
```

Hands the record back, or throws. Every path to an envelope goes through here.

### `recordJson`

```
function recordJson(record: DayRecord, catalogue?: readonly Symptom[]): string
```

The text that goes inside the envelope: canonical json of a record that passed every range.

### `recordBytes`

```
function recordBytes(record: DayRecord, catalogue?: readonly Symptom[]): Uint8Array
```

The plaintext of an envelope: a day that passed its ranges, written as canonical bytes.

### `recordFromBytes`

```
function recordFromBytes(bytes: Uint8Array): DayRecord
```

What comes back out of the envelope. The ranges are not re-checked here: a record she wrote
years ago is hers to read, and a build that refused it would lose her history rather than
protect it. The write is where a range is enforced.

## `packages/crypto/src/recovery.ts`

### `recoveryCodeLength`

```
const recoveryCodeLength = 26
```

Characters. Twenty six drawn uniformly from an alphabet of 32 carry 130 bits.

### `recoveryCodeBits`

```
const recoveryCodeBits = 130
```

Bits, written out because it is the number that says the code cannot be guessed.

### `recoverySaltLength`

```
const recoverySaltLength = 16
```

Bytes. Fresh for every account, so two women never derive one key from one code.

### `recoveryParameters`

```
const recoveryParameters = { /** Kibibytes of memory, which is the cost a graphics card cannot buy its way around. */ memoryKiB: 19456, /** Passes over that memory. */ passes: 2, /** Lanes. One, because a phone deriving this once has nothing to gain from more. */ lanes: 1, /** Bytes out, which is a vault key's length because that is what it wraps. */ derivedLength: keyLength } as const
```

Argon2id, as one constant, because contract VAULT-2 puts a ceiling on the unlock and feature 6
step 8 measures it on the slowest phone available. That step raises or lowers the memory here
and records the number it measured. Nothing else in Emi chooses these.

The values are the floor the Open Web Application Security Project recommends. They cost about
106 milliseconds on the machine this was written on, and a phone is slower.

### `wrappedVaultKeyLength`

```
const wrappedVaultKeyLength = headerLength + keyLength + tagLength
```

Bytes. A wrapped vault key is always exactly this long: the envelope header, thirty two bytes of
key, and the tag. A length that is anything else is not a wrapped key, and the service refuses it
on that alone without holding any key of its own.

### `RecoveryRefusal`

```
type RecoveryRefusal =
  | 'recovery-code-is-the-wrong-length'
  | 'recovery-code-is-not-crockford-base32'
  | 'recovery-salt-is-the-wrong-length'
  | 'random-source-is-the-wrong-length'
  | 'random-source-returned-zeroes'
  | 'wrapped-vault-key-is-the-wrong-length'
  | 'wrapped-vault-key-does-not-open';
```

Every way the recovery half refuses, as a value, so a caller reads a reason and not a message.

### `RecoveryError`

```
class RecoveryError extends Error {
  readonly refusal: RecoveryRefusal;
  constructor(refusal: RecoveryRefusal, message: string);
}
```

Carries the refusal beside the message, the way the envelope and the signature do.

### `drawRecoveryCode`

```
function drawRecoveryCode(random: RandomSource = systemRandom): string
```

A code she can write on paper. Every character is drawn from a byte, and 256 divides by 32
exactly, so masking the low five bits leaves every character as likely as every other. A modulo
of an alphabet that did not divide would quietly make the first characters more common.

### `drawRecoverySalt`

```
function drawRecoverySalt(random: RandomSource = systemRandom): Uint8Array
```

A salt of its own for every account, which is what stops one derivation serving two women.

### `readRecoveryCode`

```
function readRecoveryCode(typed: string): string
```

What she typed, read as the code she wrote down.

Case, the two letters that look like a one, the letter that looks like a zero, and the spaces
and hyphens she put in to keep her place are all hers to get wrong. A character the alphabet
genuinely does not carry is refused, and the refusal never says which character it was, because
a message that repeats the code is a message that carries the code.

### `recoveryKeyFrom`

```
function recoveryKeyFrom(code: string, salt: Uint8Array): Uint8Array
```

The key the vault key is wrapped under. It exists for as long as this call runs and is written
nowhere: the phone keeps the vault key, the server keeps the wrapped bytes, and the code that
joins them is on her paper.

### `wrapVaultKey`

```
function wrapVaultKey(vaultKey: Uint8Array, recoveryKey: Uint8Array, random: RandomSource = systemRandom): Uint8Array
```

The vault key, sealed under the recovery key, in the envelope of section 7.2.

### `openWrappedVaultKey`

```
function openWrappedVaultKey(wrapped: Uint8Array, recoveryKey: Uint8Array): Uint8Array
```

The vault key back, or nothing at all.

A wrong code derives a wrong key, the tag does not check out, and this raises. It never returns
thirty two bytes that are not her key, which is the whole reason the wrap carries a tag: a
wrapper without one would hand back rubbish and the phone would seal her next day under it.

### `readWrappedVaultKey`

```
function readWrappedVaultKey(wrapped: Uint8Array): EnvelopeParts
```

What the service is allowed to do with a wrapped key: read its shape. It holds no recovery key
and no vault key, so this is the whole of what it can check, and the fixed length is what makes
that check worth anything.

## `packages/crypto/src/signature.ts`

### `deviceKeyLength`

```
const deviceKeyLength = 32
```

Bytes. Ed25519 takes one length for a private key and one for a public key, and both are this.

### `signatureLength`

```
const signatureLength = 64
```

Bytes. Ed25519 writes a signature of exactly this length and reads no other.

### `accountIdBytes`

```
const accountIdBytes = 16
```

Bytes of the digest that the account identifier is cut from. Sixteen bytes carry 128 bits.

### `accountIdLength`

```
const accountIdLength = 26
```

Characters. Crockford base 32 writes 128 bits in this many, with two bits of padding left.

### `instantWindowSeconds`

```
const instantWindowSeconds = 300
```

Seconds. A request signed further from now than this is refused, in either direction, so a
captured request stops working soon after it is captured.

### `accountHeader`

```
const accountHeader = 'emi-account'
```

The header that names the account. It is one of the three the api reads as an identity source.

### `instantHeader`

```
const instantHeader = 'emi-instant'
```

The header that carries the instant the request was signed at.

### `signatureHeader`

```
const signatureHeader = 'emi-signature'
```

The header that carries the signature itself, as base 64.

### `bodyHashHeader`

```
const bodyHashHeader = 'emi-body-sha256'
```

The header that carries the digest of the body. A request authorizer never receives a body, so
the digest travels beside the signature and the function behind the api compares it against the
body it did receive.

### `crockfordAlphabet`

```
const crockfordAlphabet = '0123456789ABCDEFGHJKMNPQRSTVWXYZ'
```

Crockford base 32, which has no letter that a person can read as a digit. The account identifier
is the one value in this product a woman might ever read aloud.

### `SignatureRefusal`

```
type SignatureRefusal =
  | 'header-is-missing'
  | 'instant-is-not-an-instant'
  | 'instant-is-not-fresh'
  | 'signature-is-not-readable'
  | 'signature-does-not-verify'
  | 'signature-was-used-before'
  | 'body-hash-does-not-match';
```

Every way a signature is refused, as a value. An unknown account is absent from this list on
purpose: it is answered with `signature-does-not-verify`, because a caller who can tell the two
apart can ask this api which accounts exist.

### `refusalMessages`

```
const refusalMessages: Readonly<Record<SignatureRefusal, string>>
```

What a refused request is told, by refusal. The words for an unknown account are the words for a
bad signature, and the service holds that by answering with the same refusal value.

### `SignatureError`

```
class SignatureError extends Error {
  readonly refusal: SignatureRefusal;
  constructor(refusal: SignatureRefusal, message: string);
}
```

Carries the refusal beside the message, so a caller matches the reason and not the words.

### `DeviceKeyPair`

```
interface DeviceKeyPair {
  readonly privateKey: Uint8Array;
  readonly publicKey: Uint8Array;
}
```

A private key and the public key that comes from it. The private half never leaves the phone.

### `PresentedSignature`

```
interface PresentedSignature {
  readonly accountId: string;
  readonly instant: string;
  readonly signature: string;
  readonly bodyHash: string;
}
```

The four values a signed request carries in its headers.

### `RequestToSign`

```
interface RequestToSign {
  readonly method: string;
  readonly path: string;
  readonly instant: string;
  readonly bodyHash: string;
}
```

What is signed: the method, the path, the instant, and the digest of the body.

### `hexOf`

```
function hexOf(bytes: Uint8Array): string
```

Bytes written as lowercase hexadecimal, which is how a digest travels in a header.

### `crockfordBase32`

```
function crockfordBase32(bytes: Uint8Array): string
```

Crockford base 32 of the bytes, most significant bit first, with the tail padded by zero bits.
Sixteen bytes therefore give 26 characters.

### `accountIdFor`

```
function accountIdFor(publicKey: Uint8Array): string
```

Who she is, to the server: the first sixteen bytes of the digest of her public key. The server
learns nothing from it, because it is derived from a value she chose at random on her own phone.

### `deviceKeyPairFrom`

```
function deviceKeyPairFrom(privateKey: Uint8Array): DeviceKeyPair
```

The public half of a private key she already holds, so a reinstall recovers the whole pair.

### `newDeviceKeyPair`

```
function newDeviceKeyPair(random: RandomSource): DeviceKeyPair
```

A new key pair from the platform's own generator. This is the whole of making an account.

### `bodyHashOf`

```
function bodyHashOf(body: string | Uint8Array): string
```

The digest of the body, as lowercase hexadecimal. An empty body has a digest like any other.

### `signingString`

```
function signingString(request: RequestToSign): string
```

What the key signs: four lines, in this order, joined by a newline. The path carries the query
string where the request has one, because a cursor a caller can change is a cursor nobody signed.

### `signRequest`

```
function signRequest(request: RequestToSign, privateKey: Uint8Array): string
```

The signature over that string, as base 64.

### `signedHeaders`

```
function signedHeaders(keyPair: DeviceKeyPair, request: RequestToSign): Readonly<Record<string, string>>
```

The four headers a signed request carries, ready to be given to the transport.

### `presentedSignatureIn`

```
function presentedSignatureIn(headers: Readonly<Record<string, string | undefined>>): PresentedSignature | null
```

Reads the four headers back. A header name arrives lowercase from the api and from a browser, so
the reader lowercases what it is given rather than trusting the case it was sent in.

### `instantAt`

```
function instantAt(instant: string): number | null
```

The instant, as milliseconds, or null when the text is not the one written form. One written
form, so two texts can never stand for one moment.

### `instantIsFresh`

```
function instantIsFresh(instant: string, now: Date): boolean
```

Whether the instant is inside the window, in either direction. A clock that runs ahead is as much
of a problem as one that runs behind, and neither one may open the window wider.

### `signatureVerifies`

```
function signatureVerifies(presented: PresentedSignature, method: string, path: string, publicKey: Uint8Array): boolean
```

Whether the signature is this public key's signature over this request. A signature of the wrong
length is refused rather than thrown, because the caller chose the length and every refusal here
has to cost the same.

### `decoyPublicKey`

```
const decoyPublicKey: Uint8Array
```

A public key nobody holds the private half of, verified against when the account is unknown. The
work is the point: without it, an unknown account answers faster than a bad signature, and the
difference in the timing is an index of who holds an account here.
