// The colour, type and space values arrive in feature 1 step 1. The package exists now so that
// every other workspace can depend on it from the first commit.
export const packageName = '@emi/tokens';
