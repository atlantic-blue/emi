# `@emi/ui`

The exported surface of `packages/ui`, generated by `npm run generate:reference`.
Nobody writes this page by hand. The prose under each symbol is the documentation comment that
sits on it in the source, so this page cannot say one thing while the code says another.

17 exported symbols, across 10 files.

## `packages/ui/src/index.ts`

### `packageName`

```
const packageName = '@emi/ui'
```

The chrome every screen is drawn from. A screen imports a primitive from here and never copies
one, so the product has one Box, one Text and one dock rather than fifty hand rolled ones.

## `packages/ui/src/BottomNavigation.tsx`

### `NavigationTab`

```
interface NavigationTab {
  /** What the route is called, which is what the caller gets back and what the test presses. */
  readonly name: string;
  /** The word under the drawing, already in her language. */
  readonly label: string;
  readonly icon: IconName;
}
```

One column of the dock.

### `bottomNavigationTestID`

```
const bottomNavigationTestID = 'bottom-navigation'
```

The dock itself, which a test asks for to prove it is drawn, or that it is not.

### `dockPanelTestID`

```
const dockPanelTestID = 'bottom-navigation-panel'
```

The panel inside it, which carries the height, the corner, the hairline and the fill.

### `standOffTestID`

```
const standOffTestID = 'bottom-navigation-stand-off'
```

The box that stands the panel off the edges of the glass and off the foot.

### `tabTestID`

```
function tabTestID(name: string): string
```

One column, by the name of the route it reaches.

### `dockHeight`

```
const dockHeight = 64
```

Points. The height the design system fixes the dock at, with the safe area accommodated.

### `dockStandOff`

```
const dockStandOff = 12
```

Points. The air between the panel and the bottom edge of the glass.

### `deepestHomeIndicator`

```
const deepestHomeIndicator = 34
```

Points. The deepest home indicator a phone keeps for itself, which the dock pads itself by.

Nothing here read it off a device. It is the inset an iPhone with a dynamic island reports, and
the safe area fixture hands the same number to the provider, so the room the dock claims is held
against the room it draws rather than trusted.

### `dockRoom`

```
const dockRoom = dockHeight + dockStandOff + deepestHomeIndicator
```

The room a screen leaves at its foot so the dock covers nothing she can read, which is what the
dock draws on the phone that keeps the most glass for itself.

### `TheDockHangsOver`

```
function TheDockHangsOver({ children }: { readonly children: ReactNode }): ReactNode
```

Says that a dock hangs over the screens inside it, so each of them leaves room at its foot.
A screen the dock never reaches asks and is told nothing, and reserves nothing.

### `useRoomAtTheFoot`

```
function useRoomAtTheFoot(): number | undefined
```

The room at the foot of this screen, or nothing where no dock hangs over it.

### `BottomNavigation`

```
function BottomNavigation({ tabs, chosen, onChoose }: Props): ReactNode
```

The dock, drawn from the tabs it is handed, with the one she is on lit.

## `packages/ui/src/Icon.tsx`

### `Icon`

```
function Icon({ name, size = ICON_SIZE, colour, testID }: Props): ReactNode
```

One drawing from the set, at the size and the colour the screen asks for.

## `packages/ui/src/floating.ts`

### `floatingShadow`

```
const floatingShadow = `0px 4px 20px -2px ${colour.onSurface}0d`
```

The one shadow this design system allows, on layer 2 and nowhere else.

The body text colour at five per cent, four points down, blurred twenty and drawn two points
inside the edge. React Native reads the string the way a browser does, which is the only shape
that carries the spread.

Everything below layer 2 is separated by a tonal step and a hairline instead, so a second use of
this is a change to the design and not a change to a screen.

## `packages/ui/src/gluestack/box/index.tsx`

This file exports nothing.

## `packages/ui/src/gluestack/gluestack-ui-provider/index.tsx`

### `ModeType`

```
type ModeType = 'light' | 'dark' | 'system';
```

Which colour scheme the components draw in. Emi is drawn in light and asks for nothing else.

### `GluestackUIProvider`

```
function GluestackUIProvider({ mode = 'system', ...props }: { mode?: ModeType; children?: React.ReactNode; style?: ViewProps['style']; })
```

The root the copied components expect above them. It holds the overlay and the toast layers, so
a component that opens either one has somewhere to put it.

## `packages/ui/src/gluestack/hstack/index.tsx`

This file exports nothing.

## `packages/ui/src/gluestack/pressable/index.tsx`

This file exports nothing.

## `packages/ui/src/gluestack/text/index.tsx`

This file exports nothing.

## `packages/ui/src/gluestack/vstack/index.tsx`

This file exports nothing.
