# `@emi/cycle`

The exported surface of `packages/cycle`, generated by `npm run generate:reference`.
Nobody writes this page by hand. The prose under each symbol is the documentation comment that
sits on it in the source, so this page cannot say one thing while the code says another.

87 exported symbols, across 7 files.

## `packages/cycle/src/index.ts`

### `packageName`

```
const packageName = '@emi/cycle'
```

The application imports this to prove the workspace resolves at run time. A type import cannot
do it, because the types come off before the bundle runs.

## `packages/cycle/src/confidence.ts`

### `Citation`

```
interface Citation {
  readonly source: string;
  readonly doi: string;
  /** The figure the edge is computed from, in the words the paper reports it. */
  readonly figure: string;
}
```

Where a number came from. The figure is quoted in the words the paper reports it in, so a reader
can check an edge against the source rather than trust it.

### `CYCLE_LENGTH_VARIATION`

```
const CYCLE_LENGTH_VARIATION: Citation
```

The one study both band edges are derived from: 612,613 cycles from 124,648 users. Each edge
below is arithmetic on the two figures it reports.

### `POPULATION_SPREAD_DAYS`

```
const POPULATION_SPREAD_DAYS = 2.6
```

The mean of the per-user variation, over the whole cohort.

### `POPULATION_SPREAD_DEVIATION_DAYS`

```
const POPULATION_SPREAD_DEVIATION_DAYS = 2.5
```

How widely that per-user variation itself varies across the cohort.

### `HIGH_UP_TO_SPREAD_DAYS`

```
const HIGH_UP_TO_SPREAD_DAYS = POPULATION_SPREAD_DAYS
```

FIGO System 1 also publishes a regularity bound, up to seven days for ages 26 to 41, but it
measures shortest cycle to longest cycle rather than a standard deviation. The two numbers are
not in the same unit, so it cannot set an edge here. It bounds a period's duration instead, in
cycles.ts.

### `MEDIUM_UP_TO_SPREAD_DAYS`

```
const MEDIUM_UP_TO_SPREAD_DAYS = POPULATION_SPREAD_DAYS + POPULATION_SPREAD_DEVIATION_DAYS
```

Above this her own cycles vary by more than a standard deviation past the mean of the cohort.

### `ConfidenceLevel`

```
type ConfidenceLevel = 'high' | 'medium' | 'low';
```

Three words. A percentage would offer a precision that a median over six cycles does not have.

### `ConfidenceBand`

```
interface ConfidenceBand {
  readonly level: ConfidenceLevel;
  /** Spreads above this, in days. The first band starts at a spread of nothing at all. */
  readonly aboveSpreadDays: number;
  /** Spreads up to and including this, in days. The last band has no upper edge. */
  readonly upToSpreadDays: number;
  /** How the two edges come out of the cited figure. */
  readonly derivation: string;
  readonly citation: Citation;
}
```

A band, its two edges, and the reason the edges are where they are. The derivation travels with
the band, so a screen can say why Emi is unsure and not only that it is.

### `confidenceBands`

```
const confidenceBands: readonly [ConfidenceBand, ConfidenceBand, ConfidenceBand]
```

Three bands, because the design gives her narrow, middling and wide and nothing between.

### `confidenceFor`

```
function confidenceFor(spreadDays: number): ConfidenceBand
```

Refuses a negative spread rather than reading it as the narrowest band, which is what the first
comparison below would otherwise do with it.

## `packages/cycle/src/cycles.ts`

### `Flow`

```
type Flow = 'none' | 'spotting' | 'light' | 'medium' | 'heavy';
```

The flow values of the day record, design section 6.2.

### `DayRecord`

```
interface DayRecord {
  readonly day: string;
  readonly flow?: Flow;
  /** She said this bleeding is not her period. Such a day never starts a cycle. */
  readonly bleedingIsUnexpected?: boolean;
}
```

The part of a day that decides where a cycle starts. The rest of what she logged says nothing
about that, so this package never reads it.

### `Cycle`

```
interface Cycle {
  readonly startedOn: string;
  /** The day before the next cycle starts. Null while this cycle is the one she is in. */
  readonly endedOn: string | null;
  /** Null until the next cycle starts, because the length is the distance between two starts. */
  readonly lengthDays: number | null;
  /** Bleeding days from the start onwards. Null until a recorded day closes the bleeding. */
  readonly periodDays: number | null;
}
```

What can be known from the days she gave. Three of the four fields are null while this is the
cycle she is in, because each of them needs the cycle after this one.

### `CycleRefusal`

```
type CycleRefusal = 'day-is-not-a-date' | 'day-is-written-twice' | 'spread-is-not-a-length';
```

Every way this package refuses, as a value, so a screen matches on the reason rather than on the
words of a message.

### `CycleError`

```
class CycleError extends Error {
  readonly refusal: CycleRefusal;
  constructor(refusal: CycleRefusal, message: string);
}
```

Carries the refusal beside the message. The message is for a reader and the refusal is for the
code that catches it.

### `PERIOD_MAY_RUN_FOR_DAYS`

```
const PERIOD_MAY_RUN_FOR_DAYS = 8
```

Bleeding may run for up to eight consecutive days and still be one period: the upper bound of
normal menstrual duration in FIGO System 1 (Munro, Critchley and Fraser 2018, International
Journal of Gynecology and Obstetrics 143(3):393 to 408, doi 10.1002/ijgo.12666). A bleeding day
inside that window continues the period she is already having. The next one starts a cycle.

Without this bound the second day of a period would start a cycle of one day, and a period that
pauses for a day would start one of three.

### `isBleeding`

```
function isBleeding(record: DayRecord): boolean
```

Spotting counts. The design gives her one way to say that bleeding is not her period, which is
the unexpected mark, so the amount of blood is not the thing that decides.

### `startsACycle`

```
function startsACycle(record: DayRecord): boolean
```

Bleeding she did not mark as unexpected. The mark is hers, so Emi never decides on its own that
a bleed was not a period.

### `toDayNumber`

```
function toDayNumber(day: string): number
```

Days since the first of January 1970, so that day arithmetic is addition.

### `toDay`

```
function toDay(dayNumber: number): string
```

The inverse of `toDayNumber`. It reads the number in Coordinated Universal Time, so the answer
does not move when the zone of the phone does.

### `addDays`

```
function addDays(day: string, count: number): string
```

Counts in whole days, so the hour a clock changes in the spring cannot take a day with it.

### `daysBetween`

```
function daysBetween(from: string, to: string): number
```

Whole days, and negative when the second day is the earlier one.

### `cyclesFrom`

```
function cyclesFrom(days: readonly DayRecord[]): Cycle[]
```

A cycle starts on the first bleeding day that is not marked unexpected, and ends the day before
the next such day. Days she never recorded say nothing either way, so a period is closed by a
recorded day that carries no bleeding, never by silence.

### `completeCycles`

```
function completeCycles(cycles: readonly Cycle[]): Cycle[]
```

The cycles whose length is known, because the cycle after them has started.

### `cycleLengths`

```
function cycleLengths(cycles: readonly Cycle[]): number[]
```

The lengths that are known, in the order she lived them. A cycle that is still running has none
and is left out rather than guessed at.

## `packages/cycle/src/forecast.ts`

### `FORECAST_WINDOW_CYCLES`

```
const FORECAST_WINDOW_CYCLES = 6
```

Design section 8. Six is enough to see a pattern and short enough to follow a change.

### `CYCLES_BEFORE_A_FORECAST`

```
const CYCLES_BEFORE_A_FORECAST = 2
```

Emi says nothing about a next period until two cycles are complete.

### `LUTEAL_LENGTH_DAYS`

```
const LUTEAL_LENGTH_DAYS = 13
```

Days from ovulation to the next period, until her own temperature rise measures it. The design
fixes 13. The nearest published mean is 12.4 days, 95 percent interval 7 to 17, in the paper
cited in confidence.ts.

### `MINIMUM_DAYS_EITHER_SIDE`

```
const MINIMUM_DAYS_EITHER_SIDE = 1
```

A range of no width is a single date, which the forecast contract refuses, so a woman whose six
cycles were all exactly 28 days is still told a range.

### `FERTILE_DAYS_BEFORE_OVULATION`

```
const FERTILE_DAYS_BEFORE_OVULATION = 5
```

Design section 8: five days before the estimated ovulation through one day after it.

### `FERTILE_DAYS_AFTER_OVULATION`

```
const FERTILE_DAYS_AFTER_OVULATION = 1
```

The window closes a day after the estimate, because arithmetic can put the estimate a day early.

### `DayRange`

```
interface DayRange {
  readonly from: string;
  readonly to: string;
}
```

Two days, and both of them are inside the range. A forecast is never one day, so a range is what
a screen is handed.

### `Forecast`

```
interface Forecast {
  readonly kind: 'forecast';
  /** How many complete cycles the median was taken over, two to six. */
  readonly fromCycles: number;
  readonly medianLengthDays: number;
  /** One standard deviation of those lengths, the same statistic the bands are measured in. */
  readonly spreadDays: number;
  readonly confidence: ConfidenceBand;
  /** The middle of the range. Ovulation is counted back from this day. */
  readonly expectedStart: string;
  readonly start: DayRange;
  readonly estimatedOvulation: string;
  readonly fertileWindow: DayRange;
  readonly lutealLengthDays: number;
}
```

Everything a screen needs to draw a forecast and to say how sure it is, so that no screen does
arithmetic of its own.

### `Learning`

```
interface Learning {
  readonly kind: 'learning';
  readonly completeCycles: number;
  readonly needsCycles: number;
}
```

What comes back before there are cycles enough to forecast from. It carries both counts, so a
screen can say how many are still wanted rather than saying nothing at all.

### `ForecastResult`

```
type ForecastResult = Forecast | Learning;
```

A forecast, or the reason there is not one. A caller reads `kind` first, so an empty history
cannot be drawn as a date.

### `ForecastOptions`

```
interface ForecastOptions {
  /** Step 3.4 passes the length measured from her own temperature rise. */
  readonly lutealLengthDays?: number;
}
```

What a later step measures and hands in. Left out, the fixed luteal length is used.

### `median`

```
function median(values: readonly number[]): number
```

The middle value. A long cycle after an illness moves this by at most half a place.

### `spread`

```
function spread(values: readonly number[]): number
```

One standard deviation of her own cycle lengths, over n minus one, which is the statistic the
cited cohort figure is measured in. One value has no spread to measure.

### `lastLengths`

```
function lastLengths(cycles: readonly Cycle[]): number[]
```

Six lengths at most, the oldest first. A cycle from a year ago leaves the window rather than
dragging every forecast after it.

### `forecastFrom`

```
function forecastFrom(cycles: readonly Cycle[], options: ForecastOptions = {}): ForecastResult
```

The whole forecast, from her cycles and nothing else. No clock is read here, so the same days
give the same answer whenever it is asked for.

## `packages/cycle/src/moods.ts`

### `moodGroup`

```
const moodGroup: SymptomGroup
```

A mood is the mood group of the catalogue, read through its own name. She reaches for a mood
daily and for a symptom occasionally, so the log sheet gives the group its own control at the
top and a record keeps what she picks in `moods` rather than in `symptoms`.

There is one catalogue and one slug for each thing in it. A second list of mood words would
drift from this one, and a day that named the same feeling from both lists would be counted
twice by every screen that reads her history.

### `moodsIn`

```
function moodsIn(catalogue: readonly Symptom[] = symptoms): readonly Symptom[]
```

Every mood the given catalogue holds, retired ones included, in catalogue order.

### `symptomsOutsideTheMoodPicker`

```
function symptomsOutsideTheMoodPicker(catalogue: readonly Symptom[] = symptoms): readonly Symptom[]
```

What the rest of the log sheet offers, which is every group the mood picker does not carry.

### `moods`

```
const moods: readonly Symptom[]
```

The moods that ship, which is the mood group of the catalogue that ships.

### `moodSlugs`

```
const moodSlugs: readonly string[]
```

The slug of each of them, which is what a record carries and what never changes.

### `loggableMoods`

```
function loggableMoods(catalogue: readonly Symptom[] = symptoms): readonly Symptom[]
```

What the picker offers her. A retired mood is readable and is never offered again.

### `findMood`

```
function findMood(slug: string, catalogue: readonly Symptom[] = symptoms): Symptom | undefined
```

Resolves a retired mood too, so a record written before it was retired still reads.

### `isKnownMood`

```
function isKnownMood(slug: string, catalogue: readonly Symptom[] = symptoms): boolean
```

Whether a slug names a mood. A symptom from another group is not one.

### `unknownMoodSlugs`

```
function unknownMoodSlugs(slugs: readonly string[], catalogue: readonly Symptom[] = symptoms): readonly string[]
```

The validator ENVELOPE-2 calls on the `moods` array of a record. It returns the offenders
rather than a boolean, so the refusal can name them. A slug from another group is an offender:
a symptom she logged belongs in `symptoms`, and only a mood belongs here.

## `packages/cycle/src/symptoms.ts`

### `SymptomGroup`

```
type SymptomGroup =
  'mood' | 'energy' | 'pain' | 'digestion' | 'skin' | 'sleep' | 'head' | 'libido';
```

The eight groups the log sheet divides the catalogue into. `symptomGroups` holds their order.

### `Symptom`

```
interface Symptom {
  readonly slug: string;
  readonly name: string;
  readonly group: SymptomGroup;
  /** The day it stopped being offered, as `YYYY-MM-DD`. Absent while it is still offered. */
  readonly retiredOn?: string;
}
```

One entry. The slug is what a record points at and it never changes. The name is display text
and may be rewritten whenever the wording is wrong.

### `symptomGroups`

```
const symptomGroups: readonly SymptomGroup[]
```

The order the log sheet shows the groups in.

### `symptoms`

```
const symptoms: readonly Symptom[]
```

The catalogue itself, and the only list of it. A screen reads this rather than keeping a copy
that goes out of date on its own.

### `symptomSlugShape`

```
const symptomSlugShape = /^[a-z][a-z0-9]*(-[a-z0-9]+)*$/
```

A slug is lowercase letters and digits, and single hyphens between them.

### `symptomSlugs`

```
const symptomSlugs: readonly string[]
```

Every slug, the retired ones too, for the test that proves no two entries ever shared one.

### `findSymptom`

```
function findSymptom(slug: string, catalogue: readonly Symptom[] = symptoms): Symptom | undefined
```

Resolves a retired symptom too, so a record written before it was retired still reads.

### `isKnownSymptom`

```
function isKnownSymptom(slug: string, catalogue: readonly Symptom[] = symptoms): boolean
```

Reads the catalogue and not the shape of the slug, so a slug that looks right and names nothing
is still refused.

### `isRetired`

```
function isRetired(symptom: Symptom): boolean
```

A retired entry is readable forever and offered never again.

### `loggableSymptoms`

```
function loggableSymptoms(catalogue: readonly Symptom[] = symptoms): readonly Symptom[]
```

What the log sheet offers her. A retired symptom is readable and is never offered again.

### `symptomsInGroup`

```
function symptomsInGroup(group: SymptomGroup, catalogue: readonly Symptom[] = symptoms): readonly Symptom[]
```

One group of the log sheet, in catalogue order. The retired entries are already out of it.

### `unknownSymptomSlugs`

```
function unknownSymptomSlugs(slugs: readonly string[], catalogue: readonly Symptom[] = symptoms): readonly string[]
```

The validator ENVELOPE-2 calls on the `symptoms` array of a record. It returns the offenders
rather than a boolean, so the refusal can name them.

## `packages/cycle/src/units.ts`

### `TemperatureUnit`

```
type TemperatureUnit = 'celsius' | 'fahrenheit';
```

Degrees, in the two scales a thermometer sold to her is marked in.

### `WeightUnit`

```
type WeightUnit = 'kilograms' | 'pounds';
```

The two a scale sold to her reads in. A stone is a pound count and is not offered yet.

### `temperatureUnits`

```
const temperatureUnits: readonly TemperatureUnit[]
```

Everything a field may offer her for a temperature, in the order it offers them.

### `storedTemperatureUnit`

```
const storedTemperatureUnit: TemperatureUnit
```

What a day carries, from section 6.2 of the design, whichever scale she reads.

### `weightUnits`

```
const weightUnits: readonly WeightUnit[]
```

Everything a field may offer her for a body weight, in the order it offers them.

### `storedWeightUnit`

```
const storedWeightUnit: WeightUnit
```

What a day carries for a body weight, again from section 6.2 and again whatever she reads.

### `lowestTemperatureCelsius`

```
const lowestTemperatureCelsius = 34
```

Degrees. Below this a reading is a thermometer that was not held against anybody.

### `highestTemperatureCelsius`

```
const highestTemperatureCelsius = 42
```

Degrees. Above this the number came from the keypad and not from a body.

### `lowestWeightKilograms`

```
const lowestWeightKilograms = 20
```

Kilograms. The range is wide on purpose: the field catches a stray digit and it never judges
her.

### `highestWeightKilograms`

```
const highestWeightKilograms = 400
```

Kilograms, and wide for the same reason. A refusal here would lose what she typed.

### `POUND_IN_KILOGRAMS`

```
const POUND_IN_KILOGRAMS = 0.45359237
```

The international pound, which is this many kilograms by definition rather than by measurement.

### `ChosenUnits`

```
interface ChosenUnits {
  readonly temperature: TemperatureUnit;
  readonly weight: WeightUnit;
}
```

Which unit she reads each measurement in. Both default to the unit a record is written in.

### `storedUnits`

```
const storedUnits: ChosenUnits
```

Where she starts, and where a record stays: one scale for reading, one for writing.

### `toOneDecimalPlace`

```
function toOneDecimalPlace(value: number): number
```

A record carries one decimal place, so every value that reaches one passes through here. The
result is the nearest double to a number of tenths, which is the shortest text that reads back
as itself.

### `MeasurementRange`

```
interface MeasurementRange {
  readonly lowest: number;
  readonly highest: number;
}
```

The lowest and the highest a measurement may be, in the unit it is read in.

### `MeasurementReading`

```
interface MeasurementReading<Unit extends string = string> {
  readonly typed: string;
  /** The unit she typed it in, so a reading knows whether it is still the one on screen. */
  readonly readIn: Unit;
  /** Absent while the day holds no measurement, and absent while the text is refused. */
  readonly stored?: number;
  /** Absent while the text is a measurement she can save, or while she has typed nothing. */
  readonly refusal?: string;
}
```

What she typed, what will be stored, and why it was refused. All three travel together because
a field that dropped the text and kept the number would rewrite her keystrokes as she made them.

### `Measurement`

```
interface Measurement<Unit extends string> {
  /** How a refusal names it, in the wording a record uses. */
  readonly field: string;
  readonly units: readonly Unit[];
  readonly storedUnit: Unit;
  readonly range: MeasurementRange;
  readonly symbols: Readonly<Record<Unit, string>>;
  readonly names: Readonly<Record<Unit, string>>;
  readonly shownIn: (stored: number, unit: Unit) => number;
  readonly storedFrom: (shown: number, unit: Unit) => number;
}
```

Everything that differs between a temperature and a weight, so the two behave the same way
without the behaviour being written twice.

### `temperature`

```
const temperature: Measurement<TemperatureUnit>
```

Basal body temperature, in the two scales, with the range a body can produce.

### `weight`

```
const weight: Measurement<WeightUnit>
```

Body weight, in the two scales, with a range wide enough to catch only a stray digit.

### `rangeIn`

```
function rangeIn<Unit extends string>(measurement: Measurement<Unit>, unit: Unit): MeasurementRange
```

The range as a number she can type. Each end is the nearest value at one decimal place that the
record still accepts, so every number the refusal names is a number she can enter. The cost is
that a fraction beyond each end is accepted without being named, and a range that refused what
it offered would be worse.

### `typedFor`

```
function typedFor<Unit extends string>(measurement: Measurement<Unit>, stored: number | undefined, unit: Unit): string
```

The text a field starts with: her own measurement in her own unit, or nothing at all.

### `readingOf`

```
function readingOf<Unit extends string>(measurement: Measurement<Unit>, typed: string, unit: Unit): MeasurementReading<Unit>
```

What one field makes of what she typed. Nothing typed is nothing logged rather than a refusal,
because a day without a temperature is an ordinary day.

### `temperatureReading`

```
function temperatureReading(typed: string, unit: TemperatureUnit): MeasurementReading<TemperatureUnit>
```

What the temperature field makes of what she typed, in the scale she is reading.

### `weightReading`

```
function weightReading(typed: string, unit: WeightUnit): MeasurementReading<WeightUnit>
```

The same for the weight field, so both behave alike on the same keystrokes.

### `readingIn`

```
function readingIn<Unit extends string>(measurement: Measurement<Unit>, reading: MeasurementReading<Unit>, unit: Unit): MeasurementReading<Unit>
```

The same reading, as it stands in the unit she is reading now. Her stored number travels and the
text is drawn again from it, so switching the unit converts what she sees without a keystroke of
hers being converted underneath her. A refusal belongs to the text that earned it, so it does
not travel.
