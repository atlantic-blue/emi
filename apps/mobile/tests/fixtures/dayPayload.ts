/**
 * The application's own encoder, so a test reads a payload the way a phone reads it. The one
 * implementation lives beside the table it is written into.
 */
export { decodeDay, encodeDay } from '../../src/data/dayPayload';
export type { RecordedDay } from '../../src/data/dayPayload';
